import React from 'react'
import styled from 'styled-components'

export default function WatchPage() {
  return <WatchWrapper></WatchWrapper>
}

const WatchWrapper = styled.main`
  width: 100%;
  height: calc(100vh - 50px - 82px);
  position: relative;
`
